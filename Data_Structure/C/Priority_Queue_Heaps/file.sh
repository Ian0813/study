# /bin/bash

if [ -f priority_heaps.h ]
then
	echo "priority_heaps.h already exist."
else
	touch priority_heaps.h
fi


if [ -f priority_heaps.c ]
then
	echo "priority_heaps.c already exist."
else
	touch priority_heaps.c	
fi
